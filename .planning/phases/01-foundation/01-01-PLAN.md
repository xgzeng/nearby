---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Cargo.toml, src/config.rs]
autonomous: true
requirements: [TOML, SCHEMA, STACK, FILE-PERM]

must_haves:
  truths:
    - "Config structures can be serialized to TOML"
    - "Configuration file is created with 0600 permissions"
    - "Configuration directory is created with 0700 permissions"
  artifacts:
    - path: "Cargo.toml"
      provides: "Dependencies for TUI and serialization"
    - path: "src/config.rs"
      provides: "Serialize implementation and secure save logic"
  key_links:
    - from: "src/config.rs"
      to: "toml crate"
      via: "serialization"
---

<objective>
Setup the foundation for configuration persistence and serialization.
- Add necessary dependencies to Cargo.toml.
- Enable serialization for existing configuration structs.
- Implement secure file saving with proper Unix permissions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md
@src/config.rs
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Update dependencies in Cargo.toml</name>
  <files>Cargo.toml</files>
  <action>
    - Add `inquire = "0.7"` for TUI interactions.
    - Add `toml = "0.8"` for configuration serialization.
    - Update `serde` to include `derive` feature: `serde = { version = "1.0", features = ["derive"] }`.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Dependencies added and project compiles.</done>
</task>

<task type="auto">
  <name>Enable serialization and Clone for config structs</name>
  <files>src/config.rs</files>
  <action>
    - Import `serde::Serialize`.
    - Add `#[derive(Serialize, Clone)]` to `ConfigData`, `Connection`, `BLEConnection`, `Action`, and `ProximityAction`.
    - Ensure `#[serde(skip)]` is added to any fields that should not be serialized (e.g., live RSSI in `BLEConnection` if applicable, although current struct has it as `Option<i16>`). Actually, the research suggests matching `ConfigData` structure.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Config structs are serializable.</done>
</task>

<task type="auto" tdd="true">
  <name>Implement and test secure config saving</name>
  <files>src/config.rs</files>
  <behavior>
    - `save_config` should create the parent directory with 0700 permissions if it doesn't exist.
    - `save_config` should create the config file with 0600 permissions.
    - `save_config` should serialize `ConfigData` to TOML accurately.
  </behavior>
  <action>
    - Implement `save_config(path: &Path, data: &ConfigData) -> anyhow::Result<()>` in `src/config.rs`.
    - Use `std::os::unix::fs::{DirBuilderExt, OpenOptionsExt}` to set permissions (0700 for dir, 0600 for file).
    - Add a `#[cfg(test)]` block in `src/config.rs` with tests for:
        1. Serialization round-trip (Serialize then Deserialize).
        2. File permission verification using `std::fs::metadata` and `std::os::unix::fs::MetadataExt`.
  </action>
  <verify>
    <automated>cargo test config::tests</automated>
  </verify>
  <done>Secure saving is implemented and verified by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Application -> Filesystem | Writing configuration data to the user's home directory. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | config.toml | mitigate | Use 0600 permissions to prevent other users from reading or modifying the file. |
| T-01-02 | Information Disclosure | config.toml | mitigate | Use 0600 permissions to protect potentially sensitive MAC addresses or device names. |
</threat_model>

<success_criteria>
- Project compiles with new dependencies.
- `ConfigData` can be serialized to TOML.
- `save_config` creates files/dirs with restricted permissions (0600/0700).
- Unit tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
