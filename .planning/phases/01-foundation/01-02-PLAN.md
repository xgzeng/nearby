---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/main.rs, src/setup/mod.rs]
autonomous: true
requirements: [WIZARD, TOOLING, PERM]

must_haves:
  truths:
    - "Application accepts 'setup' sub-command"
    - "Application performs Bluetooth permission check on startup"
    - "Setup command launches a welcome screen using inquire"
  artifacts:
    - path: "src/setup/mod.rs"
      provides: "TUI wizard entry point"
    - path: "src/main.rs"
      provides: "CLI sub-command routing and permission checks"
  key_links:
    - from: "src/main.rs"
      to: "src/setup/mod.rs"
      via: "function call run_wizard()"
---

<objective>
Setup the CLI sub-command structure and implement the TUI wizard entry point.
- Refactor `src/main.rs` to support `setup` and `run` sub-commands.
- Implement a Bluetooth permission check to provide better error messages.
- Create the initial TUI wizard with a welcome message.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Create TUI wizard entry point</name>
  <files>src/setup/mod.rs</files>
  <action>
    - Create `src/setup/mod.rs`.
    - Implement `pub fn run_wizard() -> anyhow::Result<()>`:
        - Use `inquire::Text` or `inquire::CustomType` (or just a simple println for now) to show a welcome message.
        - Per Phase 1 success criteria: "User can start the TUI setup command and see a welcome screen."
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Wizard module exists and compiles.</done>
</task>

<task type="auto">
  <name>Refactor main.rs for sub-commands and permissions</name>
  <files>src/main.rs</files>
  <action>
    - Declare `mod setup;`.
    - Define a `Commands` enum with `#[derive(clap::Subcommand)]`:
        - `Run { #[arg(short, long)] config: Option<PathBuf> }`
        - `Setup`
    - Update `Cli` struct to include `command: Option<Commands>`.
    - Implement `async fn check_bluetooth_permissions() -> anyhow::Result<()>`:
        - Try to create a `bluer::Session`.
        - Handle `bluer::ErrorKind::NotAuthorized` with a helpful message (e.g., "Add user to 'bluetooth' group").
    - In `main()`:
        - Initialize logger.
        - Call `check_bluetooth_permissions().await?`.
        - Parse CLI.
        - Match `cli.command`:
            - `Some(Commands::Setup)` => call `setup::run_wizard()?`.
            - `Some(Commands::Run { config })` | `None` => Execute existing daemon logic (move to a function if it helps clarity, or keep in-line).
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>CLI supports sub-commands and permissions are checked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>CLI sub-commands and TUI welcome screen.</what-built>
  <how-to-verify>
    1. Run `cargo run -- setup`.
    2. Confirm a welcome message is displayed.
    3. Run `cargo run` (or `cargo run -- run`).
    4. Confirm it still attempts to start the daemon (and might fail if no config).
    5. (Optional) If possible, run as a user without Bluetooth permissions to verify the error message.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Application -> BlueZ | Accessing system Bluetooth adapter via D-Bus. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Elevation of Privilege | Bluetooth Access | mitigate | Verify permissions at startup and inform user of required groups (bluetooth/lp). |
</threat_model>

<success_criteria>
- `nearby setup` displays a welcome screen.
- Permission check identifies missing Bluetooth access.
- Existing daemon logic is still accessible via `nearby` or `nearby run`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
